This folder contains DAGs from Chapter 5. The filenames and DAG ids follow the listing ids in the book. Near the end of the chapter, we demonstrate usage of the SQLExecuteQueryOperator. The Docker Compose example in this folder creates a second Postgres database so you don't have to setup things yourself when running the example. If you like, you can access it:

Host: localhost
Port: 5433
Username: airflow
Password: airflow
Database: airflow
